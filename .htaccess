# Root .htaccess - route all requests to apps/backend/www/
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Block access to everything except apps/backend/www/
    RewriteCond %{REQUEST_URI} !^/apps/backend/www/
    RewriteRule ^(.*)$ apps/backend/www/$1 [L]
</IfModule>
