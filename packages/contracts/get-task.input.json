{
    "type": "object",
    "properties": {
        "task_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50,
            "description": "ID of the task to look up"
        }
    },
    "required": ["task_id"],
    "additionalProperties": false
}
