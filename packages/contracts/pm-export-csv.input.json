{
    "type": "object",
    "required": ["type"],
    "properties": {
        "type": {
            "type": "string",
            "enum": ["tasks", "timesheets"],
            "description": "Typ exportu: úkoly nebo výkazy"
        }
    },
    "additionalProperties": false
}
